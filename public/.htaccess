<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteBase /

    # Samo React SPA — Laravel /api ide kroz Plesk Apache directives (ne ovdje)
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteRule ^ index.html [L]
</IfModule>
