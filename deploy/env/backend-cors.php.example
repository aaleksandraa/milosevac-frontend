<?php

/**
 * Kopiraj u milosevac-backend/config/cors.php ako fajl ne postoji.
 * Omogućava frontendu na milosevac.com da zove API na api.milosevac.com.
 */

return [
    'paths' => ['api/*', 'sanctum/csrf-cookie'],
    'allowed_methods' => ['*'],
    'allowed_origins' => array_filter([
        env('FRONTEND_URL', 'http://localhost:8080'),
        'https://milosevac.com',
        'https://www.milosevac.com',
    ]),
    'allowed_origins_patterns' => [],
    'allowed_headers' => ['*'],
    'exposed_headers' => [],
    'max_age' => 0,
    'supports_credentials' => false,
];
